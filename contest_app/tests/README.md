# Tests

pytest suite for the `contest_app/src/rats` package. No `pyproject.toml`/packaging setup
exists yet — that gets added when the Python port actually starts (see
`../todo.md`), along with the first real tests.
